#include <stdio.h>
int main()
{
    int i = 1, n;
    printf("\n  Enter the value of n");
    scanf("%d",&n);
    while (i<=n)
    {
        printf("\n%d",n);
        n--;

        /* code */
    }
    return 0;
}